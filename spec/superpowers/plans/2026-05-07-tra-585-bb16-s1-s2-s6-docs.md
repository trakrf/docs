---
ticket: TRA-585
parent: TRA-583
date: 2026-05-07
spec: ../specs/2026-05-07-tra-585-bb16-s1-s2-s6-docs-design.md
status: plan
---

# TRA-585 — BB16 S1/S2/S6 docs follow-up — implementation plan

Branch: `chore/tra-585-bb16-s1-s2-s6-docs` (already created in worktree at `/home/mike/trakrf-docs-tra-585`).

## Step 1 — design doc commit

- File: `spec/superpowers/specs/2026-05-07-tra-585-bb16-s1-s2-s6-docs-design.md` (already written)
- Commit: `docs(spec): TRA-585 BB16 S1/S2/S6 docs follow-up design`

## Step 2 — plan doc commit

- File: `spec/superpowers/plans/2026-05-07-tra-585-bb16-s1-s2-s6-docs.md` (this file)
- Commit: `docs(plan): TRA-585 BB16 S1/S2/S6 docs follow-up plan`

## Step 3 — S6 prose edit in `docs/api/errors.md`

Edit the single DELETE bullet in the Idempotency section (line 217) into two bullets that distinguish top-level resource delete from tag-association delete. See design doc for exact text.

- Verify: `git grep '404 not_found.*tags' docs/` returns no hits.
- Verify: the two new bullets render in `pnpm build` without breaking the surrounding list.
- Commit: `docs(errors): split DELETE idempotency bullet for tag-delete (TRA-585 S6)`

## Step 4 — spec refresh

- Run `bash scripts/refresh-openapi.sh` against `trakrf/platform@main` (default).
- Confirm `static/api/platform-meta.json` records a SHA at or after `b1c952f` (PR #265 merge commit).
- Confirm in the refreshed spec:
  - `errors.ErrorResponse` description no longer claims full RFC 7807 compliance.
  - No `security:` blocks contain `[<scope>]` arrays — they should be empty arrays or absent. (Per OpenAPI 3.0 §4.8.30.)
  - Operation `description` text on the affected ops begins with `**Required scope:**`.
  - `Location` header is declared on POST `/api/v1/assets` and POST `/api/v1/locations` 201 responses.
  - 415 is declared on every operation that accepts a body.
  - 429 + `Retry-After` is declared on `GET /api/v1/orgs/me`.
  - `DELETE /api/v1/{assets,locations}/{id}/tags/{tag_id}` declares 204 only — no 404.
  - `q` parameter description on all three list endpoints reads identically and includes `(case-insensitive)`.
- Commit: `chore(spec): refresh openapi from platform@<sha> (TRA-585)`

## Step 5 — verification

- `pnpm install` (only if lockfile changed; refresh script doesn't touch it).
- `pnpm build` — must be clean.
- `pnpm lint` — must be 0 errors.
- Visual spot-check of the rendered errors page (Idempotency section) and the spec viewer (`/api/spec` or wherever Swagger UI mounts) is not required for this PR — a build pass is sufficient since the changes are content-only.

## Step 6 — push and open PR

- `git push -u origin chore/tra-585-bb16-s1-s2-s6-docs`
- `gh pr create` against `main` with title:
  - `docs: TRA-585 BB16 S1/S2/S6 spec follow-up + spec refresh`
- Body summarizes the design's "Findings ↔ docs impact" table, the one prose edit, and the spec refresh contents (S1/S2/S3/S4/S5/S6/S10).

## Out of scope

Same as the design doc: no oauth2 swap, no param renames (TRA-586), no `readOnly` annotations (TRA-587), no quickstart sweep (TRA-584), no handler-side parent-missing 404 cleanup, no spec backfill for prior PRs.

## Risks

- **Spec refresh pulls unexpected drift.** Mitigation: visually diff `static/api/openapi.yaml` after refresh and confirm the deltas correspond to the seven BB16 findings only. If anything else changed, surface it before committing.
- **Idempotency bullet wording drifts from spec operation-description text over time.** Mitigation: keep the wording aligned with the spec's note (`returns 204 whether or not the tag was associated`). Future spec refreshes that change the operation-description wording should re-check this bullet.
